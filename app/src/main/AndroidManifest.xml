<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="chimera.epic_rpg_thing">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.combatActivity"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".view.storyActivity"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".view.selectSkillsActivity"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".view.newCharacterActivity"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".view.newGameActivity"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".view.MainActivity"
            android:screenOrientation="sensorLandscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>